# Nebula 项目结构说明

## 项目概述

**Nebula** 是一个基于 BepInEx 框架的 Among Us 大型模组，版本 2.3.1。该项目主要添加了自定义职业、角色属性、游戏模式、地图编辑等丰富的游戏内容。

---

## 主要文件夹说明

### 核心运行时模块

---

#### 📁 NebulaAPI (API 接口库)

**定位：供外部脚本调用的公开 API 接口**

提供了一套完整的 API，允许开发者编写自定义脚本和扩展功能。命名空间为 `Virial`。

| 子目录 | 功能说明 |
|---|---|
| `Accessibility/` | 辅助功能，覆盖层管理 |
| `Assignable/` | 可分配系统，角色分配、队伍、模板等 |
| `Attributes/` | Nebula 特性标记 |
| `Command/` | 命令系统，任务、令牌、执行器 |
| `Common/` | 通用类，生命周期、权限 |
| `Compat/` | 兼容性层，颜色、输入、选择等 |
| `Components/` | 组件，游戏计时器、HUD 内容、按钮等 |
| `Configuration/` | 配置系统，角色过滤器 |
| `DI/` | 依赖注入容器 |
| `Events/Game/` | 事件系统，游戏开始/结束/更新事件 |
| `Exception/` | 异常处理 |
| `Game/Object/` | 游戏核心类，玩家、地图、模式、动作等 |
| `Helpers/` | 辅助类，数组、字符串、文本处理 |
| `Internal/` | 内部实现，Among Us 低级接口 |
| `Logging/` | 日志接口 |
| `Map/` | 地图组件 |
| `Media/` | 媒体资源，GUI、图像、音乐、本地化 |
| `Runtime/` | 运行时管理 |
| `Text/` | 文本处理，标签系统 |
| `Utilities/` | 工具类，位掩码、缓存、哈希、路径查找等 |
| `VisualProgramming/` | 可视化编程，电路、节点、数值 |

**核心文件：**
- `NebulaAPI.cs` - API 主入口
- `ModuleFactory.cs` - 模块工厂

---

#### 📁 NebulaPluginNova (主插件实现)

**定位：实际加载到游戏中的插件主体**

这是最终编译后运行的插件，整合了所有功能，包含大量自定义职业、游戏逻辑和补丁。

| 子目录 | 功能说明 |
|---|---|
| `AeroGuesser/` | 地图猜测器功能（计分、计时） |
| `Behavior/` | 行为模块，开发工作室、市场、动画等 |
| `Commands/` | 命令系统实现 |
| `Dev/` | 开发工具，音频播放器、VC、编辑器等 |
| `Game/` | 游戏逻辑，条件检查、地图、模式、击杀请求等 |
| `Map/` | 地图数据（包含 Fungle 新地图） |
| `Modules/` | 功能模块（成就、配置、调试、语言、同步等） |
| `Patches/Old/` | 旧版补丁文件 |
| `Player/` | 玩家逻辑，假玩家、光照信息、任务状态 |
| `Resources/` | 资源文件（GUI、地图、图标等大量图片） |
| `Roles/Complex/` | 复杂职业：猜测者、核心、秘密、交换者、陷阱师 |
| `Roles/Crewmate/` | 船员职业：特工、诱饵、街头艺人等 12 个职业 |
| `Roles/Impostor/` | 内鬼职业：外星人、大炮、丘比特等 13 个职业 |
| `Roles/Modifier/` | 修饰词：嗜血、诅咒、恋人等 |
| `Roles/Neutral/` | 中立职业：复仇者、舞者、赌徒等 12 个职业 |
| `Roles/Perks/` | 属性技能：脚印、钩爪、隐身等 7 个 |
| `Scripts/` | CSScript 脚本系统 |
| `Utilities/` | 工具类，版本控制、网络请求、对象池等 |
| `VoiceChat/` | 语音聊天功能 |

**核心文件：**
- `Nebula.cs` - 插件入口
- `NebulaManager.cs` - 游戏管理器
- `NebulaImpl.cs` - 实现层
- `NebulaAsset.cs` / `VanillaAsset.cs` - 资产管理
- `ResidentBehaviour.cs` - 常驻行为

---

#### 📁 NebulaLoader (加载器)

**定位：自动更新和版本管理的加载器**

负责在游戏启动时检查版本一致性、自动下载更新，并加载 Nebula.dll。

**主要功能：**
- 检测 Among Us 游戏版本（通过 GameAssembly.dll 大小）
- 从 GitHub 获取最新版本信息
- 自动下载匹配的 Nebula.dll
- 版本不一致时的自动更新
- URL 转换器（用于网络受限环境）

---

### 工具与辅助模块

---

#### 📁 NebulaInstaller (安装器)

**定位：WPF 图形界面安装程序**

一个 Windows 桌面应用程序，提供图形化安装界面。

**页面流程：**
- `StartPage` - 开始页面
- `SelectVanillaPage` - 选择原版游戏目录
- `SelectInstallToPage` - 选择安装位置
- `InstallingPage` - 安装进度页面
- `FinishPage` - 完成页面
- `FailedPage` - 失败页面

---

#### 📁 NebulaBuilder (构建工具)

**定位：项目构建辅助工具**

用于自动化编译和打包流程的命令行工具。

---

#### 📁 NebulaAndroid (Android 支持)

**定位：Android 平台的兼容性支持**

提供 Android 平台的脚本编译器实现，使模组能在移动端运行。

| 文件 | 功能说明 |
|---|---|
| `CSScriptAndroid/Compiler.cs` | Android 平台的 C# 脚本编译器 |
| `NebulaAndroidPlugin.cs` | Android 插件入口 |

---

#### 📁 AddonScriptCompiler (附加脚本编译器)

**定位：自定义脚本编译工具**

允许用户编写自定义脚本并编译为模组可用的格式。

| 文件 | 功能说明 |
|---|---|
| `Compiler.cs` | 编译器实现 |
| `Program.cs` | 入口程序 |

---

#### 📁 AddonScriptLoader (附加脚本加载器)

**定位：运行时脚本加载**

负责在游戏运行时加载和编译用户自定义脚本。

| 文件 | 功能说明 |
|---|---|
| `ScriptCompiler.cs` | 脚本编译器 |

---

#### 📁 CPUAffinityEditor (CPU 亲和性编辑器)

**定位：CPU 核心绑定工具**

一个独立的小工具，用于设置 Among Us 进程的 CPU 亲和性，优化游戏性能。

---

#### 📁 ResourceRenamer (资源重命名工具)

**定位：DLL 嵌入资源名称修复工具**

使用 Mono.Cecil 库修改程序集的嵌入资源名称，将资源名中的反斜杠 `\` 替换为点号 `.`，修复 Windows 路径格式导致的资源加载问题。

---

#### 📁 SimpleModdedEnv (简单模组环境测试)

**定位：模组环境验证测试插件**

一个小型的 BepInEx 插件（命名空间 `DollyProb`），用于验证模组环境是否正常工作。主要测试 IL2Cpp 协程转换和调用是否正常。

---

#### 📁 Scripts (脚本工具)

**定位：Python 辅助脚本**

包含用于处理语言文件的 Python 脚本。

| 文件 | 功能说明 |
|---|---|
| `RearrangeLang.py` | 重新排列语言文件，将源目录的键值对按照目标模板的顺序重新组织输出 |

---

#### 📁 Dll (依赖库)

**定位：第三方依赖库**

包含项目所需的各类 .NET 库文件：
- Microsoft.CodeAnalysis 系列（C# 编译）
- System.* 系列（.NET 基础库）
- Sentry.* 系列（错误跟踪）

---

## 项目架构总结

```
┌─────────────────────────────────────────────────────────┐
│                    游戏 (Among Us)                        │
└─────────────────────────────────────────────────────────┘
                          ▲
                          │
┌─────────────────────────┴─────────────────────────────┐
│                  BepInEx 框架                           │
└─────────────────────────┬─────────────────────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        ▼                 ▼                 ▼
┌───────────────┐ ┌──────────────┐ ┌──────────────┐
│ NebulaLoader  │ │  NebulaAPI   │ │NebulaPlugin  │
│  (加载/更新)   │ │  (API接口)   │ │   (主插件)    │
└───────────────┘ └──────────────┘ └──────┬───────┘
        ▲                                 │
        │         ┌───────────────┐       │
        └─────────│   Nebula      │◄──────┘
                  │  (核心库)      │
                  └───────────────┘


【辅助工具链】
┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ Installer    │ │   Builder    │ │  Android     │ │  CPU Affinity│
│  (安装)      │ │  (构建)      │ │  (移动端)    │ │  (性能优化)  │
└──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘

┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ ScriptComp   │ │ ScriptLoader │ │ Resource     │ │ SimpleEnv    │
│ (脚本编译)   │ │ (脚本加载)   │ │ Renamer      │ │ (环境测试)   │
│              │ │              │ │ (资源修复)   │ │              │
└──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘

┌──────────────┐
│   Scripts    │
│ (Python工具) │
└──────────────┘
```

**数据流向：**
1. **NebulaLoader** 首先运行，检查更新并加载 Nebula.dll
2. **NebulaPluginNova** 是最终运行的插件，依赖 NebulaAPI 和 Nebula 核心库
3. **NebulaAPI** 提供扩展接口，允许外部脚本调用
4. **NebulaInstaller** 是独立的安装程序，不参与运行时
